// These are data types that are used in the application


//dta types
/* 
Boolean = TRUE /FALSE
strings = 'Hello;
Numbers = 87;
Null = no value
Undefined = variable has been declared but not assigned a value;
object = {name: 'Jesus' , age: 43};
symbol = unique identifiers
BigInt = large numbers

*/
let grannyAge = 32;
let str = "Hello";
let str2 = "Single quotes are ok too";
let phrase = `can embed another ${str}`;
console.log(phrase);
// backticks are used to embed variables in a string
// `USE backticks to insert variables in a string`

let age = null;  // value is unknown


let years ;
console.log(years); // undefined This is because the variable has been declared but not assigned a value